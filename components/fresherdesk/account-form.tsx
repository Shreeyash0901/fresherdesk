"use client";
import { useState } from "react";import Link from "next/link";import { Eye,EyeOff,ArrowRight,GraduationCap } from "lucide-react";
import { Input } from "@/components/ui/input";
export function AccountForm({signup=false}:{signup?:boolean}){
 const [visible,setVisible]=useState(false),[message,setMessage]=useState("");
 return <main className="account-page container"><div className="account-story"><span className="eyebrow">A FUTURE WORTH BUILDING</span><h1>{signup?"Your next chapter starts here.":"Good to see you again."}</h1><p>Learn something new. Build something meaningful. Take one more step toward your future.</p><div className="account-story-icon"><GraduationCap size={65} strokeWidth={1.2}/></div></div><section className="account-card"><span className="demo-badge">Frontend preview</span><h2>{signup?"Create your account":"Welcome back"}</h2><p>{signup?"Start your learning journey with FresherDesk.":"Make a little more progress today."}</p><form onSubmit={e=>{e.preventDefault();setMessage(signup?"Account creation is not connected yet. You can explore the demo workspace below.":"Sign-in is not connected yet. You can explore the demo workspace below.");}}>{signup&&<div className="form-field"><label htmlFor="name">Full name</label><Input id="name" autoComplete="name" required placeholder="Your name"/></div>}<div className="form-field"><label htmlFor="email">Email address</label><Input id="email" autoComplete="email" type="email" required placeholder="you@example.com"/></div><div className="form-field"><label htmlFor="password">Password</label><div className="password-field"><Input id="password" autoComplete={signup?"new-password":"current-password"} type={visible?"text":"password"} required placeholder="Enter your password"/><button type="button" onClick={()=>setVisible(v=>!v)} aria-label={visible?"Hide password":"Show password"}>{visible?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></div><button className="button button-green" type="submit">{signup?"Create account":"Log in"}<ArrowRight size={17}/></button><p className="form-message" role="status">{message}</p></form><div className="account-divider"><span>or take a look around</span></div><Link className="button button-outline" href="/dashboard">Explore the demo workspace<ArrowRight size={17}/></Link><p className="account-switch">{signup?"Already have an account?":"New to FresherDesk?"} <Link href={signup?"/login":"/signup"}>{signup?"Log in":"Sign up"}</Link></p><p className="preview-note">These forms are a UI preview. No credentials are sent or stored.</p></section></main>;
}
